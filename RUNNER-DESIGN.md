# 设计文档 — 本地测试执行器（Local Runner）

> 上游：`HANDOFF-RUNNER.md`（决策：放弃 Kiwi，改成「本地小工具直写 Lark」）
> 本文定稿的是**怎么实现**，不改上游的结论。
> 写于 2026-09-15 ｜ 目录：`qa-toolkit/runner/`
>
> **状态：已确认（v1.0）** —— 2026-09-15 第二轮修订后定稿，可以照此开工。
> 下游唯一未定的三项（原生窗位置、卡片宽度、PiP 初始尺寸）都是视觉参数，
> **先按第 15 节的建议默认值实现**，跑起来看着不对再调，不影响架构。

---

## 〇、一页速览

| 项 | 定稿 |
|---|---|
| 后端 | Python 标准库 HTTP 服务，三个接口，复用 `scripts/lark_client.py` |
| 前端 | 原生 HTML/CSS/JS 单页，一份代码 |
| 外壳 | **两个都做**：浏览器 Document PiP（零安装）+ pywebview 原生窗（`on_top`） |
| 置顶 | 页面内按钮 → PiP 浮窗；或原生窗 `on_top=True` 常驻 |
| 数据源 | `data/cases.normalized.json`（14 条，读进来直接渲染） |
| 落库 | 同一个多维表格两张表：测试流程记录（每条都写）+ bug 报告（仅不通过提） |
| 覆盖 | 重提交走 update，**只传本次有变化的字段**；没填的字段根本不提交 → 物理上不可能空覆盖 |
| 依赖 | Python `requests`（已有）；pywebview 仅原生窗模式需要 |
| 分水岭 | 第 3 个里程碑 —— 点「通过」能在 Lark 里看到正确的一行，整条路就通了 |

---

## 一、目标与非目标

### 要做的

1. 一屏一条用例，读完点「通过 / 不通过」，**不跳页、不填表**（不通过才填）。
2. 不通过时原地展开「说明 + 控制台报错 + 截图」，截图支持**直接 Ctrl+V 粘贴**。
3. 点确认 → 结果直接进 Lark，不经过任何中间层。
4. 关掉再打开，进度还在；同一条重提交是**覆盖**不是追加。
5. 卡片窗口能**浮在被测系统之上**，测试时不用来回切窗口。

### 不做的

- 不做用例编辑（用例的源头是 `cases.normalized.json`，改源头不改工具）。
- 不做 Kiwi 相关任何事（`kiwi` 字段读进来忽略）。
- 不做多人协作 / 账号体系（单人本地工具，报告人从 `.env` 取）。
- 不做仪表盘、趋势图、报表（要看去 Lark 看，那边有视图）。

---

## 二、关键决策

| # | 决策 | 理由 |
|---|---|---|
| D1 | **不用 Electron** | 它唯一的优势是「有个图标 + 能置顶」，而置顶 PiP 和 pywebview 都能给；代价却是 150MB 产物 + 一整套 Node 工具链。上游 `HANDOFF-RUNNER.md` 已判过，维持。 |
| D2 | **必须有个后端**，不能纯前端直连 Lark | ① `app_secret` 进前端等于把整个 base 的读写权交出去；② 浏览器直连 Lark 开放平台有跨域限制。 |
| D3 | **外壳做两个，前端只一份** | 前端一旦和壳解耦，多一个壳的成本只是一个 20 行的文件。浏览器壳保证「今天就能用」，原生壳保证「长期最稳」。 |
| D4 | **置顶首选 Document PiP** | 零安装、真·always-on-top、无边框、可拖可缩放，正好命中「卡片 + 两个按钮 + 贴图」。 |
| D5 | **pywebview 作为二号壳** | `pip install` 一个几百 KB 的包，拿到 `on_top=True` 的常驻置顶 + 可固定位置尺寸。前端一行不改。 |
| D6 | **Tauri 暂不做** | 只在「要打包成 exe 发给同事」时才值得，现在做属于提前投入。好消息是届时前端原样复用。 |
| D7 | **重提交用 update，且只传变化字段**（⚠️ 与上游建议不同） | 上游写的是「先删旧记录再写新的」。改用 `update_record`：`record_id` 稳定、不会因「删成功而写失败」留下空洞、少一次 API 调用。**并且利用「未传入的字段保持不变」这条官方语义，把没填的字段直接不放进请求体 —— 空覆盖从机制上不可能发生。** 详见第八节。 |
| D8 | **日期字段只到日** | 已只读核对过 Lark：两张表的日期字段 `date_formatter` 都已是 `yyyy/MM/dd`，**不用改表结构**。代码端改为传「当天正午的毫秒」而不是当前时刻，避开日界。 |

---

## 三、架构

三种外壳，共用同一份前端：

```
Chrome 标签页          Chrome 画中画         原生窗口壳
（开发与回退）          （置顶 · 零安装）      （置顶 · pywebview）
      │                      │                      │
      └──────────────────────┼──────────────────────┘
                             ▼
                   index.html + app.js
            卡片渲染 · 状态 · 快捷键 · 贴图 · PiP 搬运
                             │
                             ▼  fetch
                        serve.py
        GET  /api/session    POST /api/submit    GET /api/health
                             │
                             ▼
                    lark_client.py（已有，直接 import）
                             │
                             ▼
                  测试流程记录 · bug 报告（同一 base）
```

**为什么这样切**：前端只认三个 HTTP 接口，不认外壳。所以「哪个壳」是纯粹的启动问题，换壳不碰业务代码。

```
Browser 模式：双击 start.bat
    → 起 serve.py → 自动开浏览器 127.0.0.1:8765 → 点页面里的「置顶」按钮

Desktop 模式：双击 start-desktop.bat
    → 起 serve.py（同一份，跑在后台线程）
    → pywebview 开一个 420×900、无边框、on_top 的原生窗指向同一个地址
```

两种模式**可以同时开**，共享同一份 `progress.json`。

---

## 四、目录结构

```
qa-toolkit/runner/
├── serve.py            后端：HTTP 服务 + 三个接口 + 写 Lark
├── app.py              pywebview 外壳（≈20 行，import serve 后开窗）
├── start.bat           浏览器模式：起服务 + 开浏览器
├── start-desktop.bat   原生窗模式：先 pip 装 pywebview，再起 app.py
├── progress.json       本地进度（运行时生成，可删；删了等于重置）
├── static/
│   ├── index.html      页面骨架
│   ├── style.css       视觉规范（第九节的落地）
│   └── app.js          状态 · 渲染 · 键盘 · 贴图 · PiP
└── shots/              提交失败时的截图暂存（跑完即清）
```

`serve.py` 通过 `sys.path` 引入 `../scripts/` 下的 `lark_client.py` 和 `envutil.py`，`.env` 读 `../.env`。

---

## 五、数据契约

### 5.1 用例来源：`data/cases.normalized.json`

```
{ "meta": {...}, "cases": [ ...14 条... ] }
```

每个 case 的字段与卡片板块的对应关系（**不需要写任何解析代码**）：

| 卡片上的位置 | JSON 字段 |
|---|---|
| 标题 | `id` + `title` |
| 元信息行 | `side` / `module` / `priority` / `layer` / `case_type` |
| 依据行 | `requirement`（`evidence_level` 作为依据强弱标签） |
| 前置条件 | `preconditions`（数组） |
| 测试数据 | `test_data`（数组） |
| 执行步骤 | `steps`（数组） |
| 预期结果 | `checkpoints`（数组，取 `.text`；`.flags` 含 `"negative"` 时标为负向断言） |
| 底部标签 | `blocker`、`pending_confirms`（数组） |
| 忽略 | `kiwi`、`order`、`stats`、`evidence_note`（`stats` 可用来显示「共 9 条」） |

### 5.2 本地进度：`progress.json`

```json
{
  "session": "2026-09-15",
  "reporter": "Max",
  "cursor": 4,
  "records": {
    "B-003": {
      "result": "不通过",
      "record_id": "recXXXXXXXX",
      "bug_record_id": "recYYYYYYYY",
      "submitted_at": 1757951234567,
      "note": "点「导出」没反应，列表还是原来的数据",
      "console": "Uncaught TypeError: ...",
      "images": [{ "name": "shot-1.png", "file_token": "boxcnXXXX" }],
      "last_fields": {
        "用例": "B-003 节点参数页档位表内容正确",
        "结果": "不通过",
        "优先级": "P0",
        "负责人": "待指派",
        "控制台": "Uncaught TypeError: ...",
        "报告人": "Max",
        "日期": 1757952000000,
        "截图": ["boxcnXXXX"]
      }
    }
  }
}
```

约定：

- **一次提交 = 一条 `records[case_id]`**，重提交原地覆盖这个对象，不新增。
- `file_token` 存下来是为了「只改文字不改图」时能复用，不必重传。
- **`last_fields` 是「上一次真正发出去的那批字段」的快照**，专门为空覆盖兜底（第八节）。它记的是**发出去的值**，不是界面上的值 —— 两者在「没填」时有区别。
- 文件损坏 / JSON 非法 → 备份成 `progress.json.bak` 后从空状态起，**不静默丢数据**。
- 这个文件是唯一真相，Lark 是它的投影。删掉它只影响「本地记得什么」，不影响 Lark 里已有的记录。

---

## 六、接口契约

### `GET /api/health`

启动时先跑这个。验三件事：token 换得到吗、两张表读得到吗、字段名对不对。

```json
// 200
{ "ok": true, "token": true, "base": "PYUkbr...",
  "tables": { "records": { "ok": true, "rows": 12 },
              "bugs":    { "ok": true, "rows": 3 } },
  "reporter": "Max", "dry_run": false }

// 503 —— 失败要给人话，不要抛栈
{ "ok": false, "step": "records", "error": "Lark 报错 code=1254040",
  "hint": "应用没被加进这张多维表格 → 打开表格 → 右上角 ··· → 添加文档应用" }
```

### `GET /api/session`

```json
{ "cases": [ ...14 条原文... ],
  "progress": { ... },
  "reporter": "Max",
  "owner": "待指派",
  "table": "测试流程记录",
  "dry_run": false }
```

### `POST /api/submit`（`multipart/form-data`）

| 字段 | 必填 | 说明 |
|---|---|---|
| `case_id` | ✓ | `B-003` |
| `result` | ✓ | `通过` / `不通过` / `未执行`（界面文案是「跳过」，见 9.7） |
| `note` | 不通过时必填 | 说明 |
| `console` | | 控制台报错原文 |
| `images` | | 0~N 个文件，同一字段名重复出现 |
| `touched` | | JSON 数组，列出**用户本次显式清空**的字段名（如 `["console"]`）。不传 = 没碰任何字段 |

**`touched` 是空覆盖防线的一环**：只有出现在 `touched` 里的字段，才允许被写成空值。见第八节。

处理顺序（**后端串行，不并发**）：

```
1. 校验 case_id / result /（仅「不通过」要求 note 非空；「跳过」什么都不用填）
2. 每个文件落临时文件 → upload_media() → file_token   （一次上传，两表共用）
3. 写 / 更新 测试流程记录  → record_id
4. **仅** 若 result == 不通过 → 写 / 更新 bug 报告 → bug_record_id
5. 更新 progress.json（cursor 前进、刷新 last_fields）
6. 清临时文件
```

```json
// 200
{ "ok": true, "record_id": "recXXX", "bug_record_id": "recYYY",
  "submitted_at": 1757951234567, "dry_run": false }

// 4xx/5xx —— 任何一步失败都返回人能看懂的话
{ "ok": false, "step": "upload|records|bugs", "error": "...", "hint": "...",
  "partial": { "record_id": "recXXX", "bug_record_id": null } }
```

**`partial` 很重要**：如果记录写成功了但 bug 写失败，要把已经拿到的 `record_id` 带回来，前端存进 `progress.json`，这样下次重提交走 update 覆盖，不会在表里留一条重复。

### 附：撤销（Ctrl+Z）不新增接口

Ctrl+Z 只做**本地回退**：把 `cursor` 退回上一条、重新打开那张卡片。
Lark 里已经写过的那条**保留**；重新提交时走覆盖逻辑（第八节）自然修正。
要真正作废一条，去 Lark 手动删 —— 本地工具不承担删数据的责任，这样更安全。

---

## 七、Lark 写入映射

两张表在**同一个多维表格**里（`LARK_BUG_APP_TOKEN`），这是硬要求：`file_token` 只在该 base 内有效，跨 base 要重传。

### 7.1 测试流程记录 `LARK_TABLE_RECORDS`（每条都写）

| 列 | 值 |
|---|---|
| 用例 | `f_text(f"{id} {title}")` |
| 结果 | `f_select(result)` → 通过 / 不通过 / **未执行**（界面上叫「跳过」） |
| 优先级 | `f_select(case.priority)` → P0~P3 原样 |
| 负责人 | `f_text(DEFAULT_OWNER)` → 「待指派」 |
| 截图 | `f_attachment(tokens)` |
| 控制台 | `f_text(console)` |
| 报告人 | `f_text(DEFAULT_REPORTER)` → **「Max」** |
| 日期 | `f_date(ts_date_ms())` ← 毫秒，但取**当天正午**，见 7.3 |

> 这一表**只负责回答「过没过」**：靠「结果」列的单选标签表达，不写说明文字。
> 要看不通过的细节，去 bug 报告查 —— 不要在这张表上加列。

### 7.2 bug 报告 `LARK_TABLE_BUGS`（只有「不通过」才写）

| 列 | 值 |
|---|---|
| 问题描述 | `f_text(f"{id} {title}" + "\n" + note)` |
| 进展状态 | `f_select("待修复")` 固定 |
| 跟进人 | 留空（等指派） |
| 优先级 | `case.priority`，**P3 归到 P2** |
| 截图 | 同一批 `file_token` |
| 反馈人 | `f_user([DEFAULT_REPORTER_ID])` ← **人员字段只认 open_id** |
| 反馈时间 | `f_date(ts_date_ms())` ← 同样只到日 |
| 备注 | `f_text("【自动提】由用例 {id} 提交（结果：{result}）" + "\n" + console)` |

### 7.3 日期怎么传（只到日）

**已只读核对过**：两张表的日期字段 `date_formatter` 都是 `yyyy/MM/dd`，
字段本身就已经是「只到日」——**不用改表结构**。要改的只有代码端传哪个毫秒值。

**别传当前时刻，传「当天正午 12:00 的毫秒」：**

```python
def ts_date_ms(d=None):
    """日期字段（只到日）用的毫秒。取当天正午，不取当前时刻。

    为什么是正午：日期字段存的是瞬时时间戳，显示时按 base 的时区渲染。
    传当前时刻的话，本地深夜或凌晨提交时，在 base 时区里可能落到前一天 / 后一天。
    正午离两个日界各 12 小时，能吸收 ±12h 的时区差 —— 这是处理
    「只到日」日期字段的常规做法。
    """
    d = (d or datetime.now()).replace(hour=12, minute=0, second=0, microsecond=0)
    return int(d.timestamp() * 1000)
```

**不能沿用 `lark_client.ts_ms()`** —— 那是「当前时刻」的口径，是给「日期时间」字段用的。

**动手时要验证一次**：提交一条，去 Lark 看显示的日期是不是当天。不对就调这里。

### 7.4 两条必须守住的不变量

1. **`【自动提】` 水印不能去掉。** 以后清理脚本只删带这个标记的记录，手工补的 bug 才不会被误伤。
2. **人员字段只认 open_id，且必须是同一个应用 token 读出来的。** `.env` 里的 `DEFAULT_REPORTER_ID` 已经是这个口径，照抄。

---

## 八、幂等与覆盖（防空覆盖）

### 8.1 事实基础

官方语义（已核实）：**更新记录时，仅传入的字段会被更新，未传入的字段保持不变。**

推论：**空覆盖的唯一来源，是我们自己主动把一个空值放进了请求体。**

所以既不需要「先删后写」，也不需要任何回滚逻辑。只要做到「没填的字段根本不提交」，空覆盖就不可能发生。

### 8.2 字段级合并规则

每次提交，对每个字段独立判断：

| 本次值 | 上次（`last_fields`） | 处理 |
|---|---|---|
| 有值 | 有 / 无 | **提交新值** |
| 空 | 有值，用户本次没碰 | **不放进请求体** → Lark 保留旧值 |
| 空 | 有值，用户在 `touched` 里声明清空 | 提交空值 ← **这是唯一允许写空的通道** |
| 空 | 空 | 不放进请求体 |

附件单独一条规则：

- 本次传了新图 → 提交**完整的新 token 列表**（附件是整体替换语义）
- 本次没传图 → **不提交「截图」字段** → Lark 里旧图原样保留

### 8.3 三道护栏

1. **本地比对（提交前，必然执行）**
   合并结果与 `last_fields` 逐字段比。若出现「上次有值 → 这次要写空」且不在 `touched` 里，
   说明合并逻辑有 bug → **拒绝提交并报错**，而不是写进去。
   这是最后一道闸：宁可报错让人看一眼，也不写空。

2. **云端比对（提交前，默认开，`--no-pull` 可关）**
   先 `GET` 该 `record_id` 的当前 fields，与 `last_fields` 比：
   - 一致 → 继续
   - 不一致 → 这条记录在别处被改过（比如别人在表里手动改了）→ **以云端为准重新合并**，
     并把差异提示出来。避免本地快照过期导致的盲目覆盖。

3. **回读校验（提交后，默认开）**
   写完立刻 `GET` 回来，确认关键字段（用例 / 结果）非空且等于本次值；不等就报错 + 重试。
   **「提交成功」的提示必须建立在回读通过之上**，不能假设写成功。

### 8.4 首次 vs 重提交

```
提交 B-003
   │
   ├─ records["B-003"] 不存在
   │     → create_record()   → 存 record_id、last_fields
   │
   └─ 已存在，且有 record_id
         → 按 8.2 合并出 diff_fields
         → diff_fields 为空（什么都没变）→ 直接返回，不请求 Lark
         → 非空 → update_record(record_id, diff_fields) → 刷新 last_fields
```

注意最后那条：**「什么都没变」就不打 Lark。** 重按一次 Enter 不该产生任何网络请求 ——
既省频控，也让「重提交」这件事没有副作用。

### 8.5 为什么改掉上游的「先删后写」

| | 删 + 写 | update + 只传变化字段（本方案） |
|---|---|---|
| API 调用 | 2 次 | 0 ~ 1 次 |
| 中途失败 | 记录已删、新记录没写成 → **数据空洞** | 原记录完好 |
| `record_id` | 每次变 | 稳定，可长期引用 |
| 空覆盖风险 | 有（新记录只有本次字段） | **无**（没填的字段不上送） |
| 附件 | 旧图连带删除 | 不传即保留，传了才替换 |

### 8.6 bug 报告那条同理

bug 报告只有「不通过」才写，字段里会变的是「问题描述」（含说明）、「截图」、「备注」（含控制台）。
同一套合并规则。

若某次把结果从不通过改成通过 —— **bug 报告不动**。要不要撤，人决定。
上游那条教训仍然生效：**不写任何自动删记录的逻辑。**

### 8.7 顺手的一个安全网

提交前 `list_records()` 拉一次目标表（与护栏 2 共用结果），如果发现同一个「用例」值有多条
（历史上用过「删+写」留下的重复），提示出来让人决定，**不自动删**。

---

## 九、界面设计

### 9.1 设计目标

「漂亮」的落点不是配色，是**判断速度**：

- 读完一条用例要几秒？→ 四块内容固定顺序，不折叠、不换位，用久了肌肉记忆。
- 还剩几条？→ 顶部 14 格进度条，不用算、不用问。
- 哪条断言和别的不是一回事？→ 负向断言（不该出现的元素）单独染色。
- 手要不要离开键盘？→ 不用，见 9.4。

### 9.2 主态

```
┌──────────────────────────────────────────────────────────┐
│ ███████░░░░░░░  14 格进度（已过=绿 / 当前=琥珀 / 未到=灰） │
│                                                          │
│ B-003 · 节点参数                            3 / 14        │
│ 后台 / 节点参数 / P0 / 冒烟层 / 主流程                    │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ 依据：后台 PRD §3.1 / §4.5 F-05                       │ │
│ └──────────────────────────────────────────────────────┘ │
│                                                          │
│ 前置条件                                                  │
│   系统管理员登录                                          │
│                                                          │
│ 测试数据                                                  │
│   无                                                     │
│                                                          │
│ 执行步骤                                                  │
│   1) 进入 F-05 节点参数页                                 │
│                                                          │
│ 预期结果  共 9 条                                         │
│   1  页面构成：三类奖励说明条 → 节点档位与奖励比例表 →     │
│      规则变更记录，三段齐全                                │
│   2  不出现「导出」按钮（负向断言，琥珀色）                │
│   …  其余 7 条滚动查看                                    │
│                                                          │
│ [ 待确认 · PRD §4.5 列头笔误 ]                            │
│──────────────────────────────────────────────────────────│
│   Enter 通过　Backspace 不通过　← → 切换　Ctrl+Z 撤销      │
│   ┌────────────────────┐ ┌────────────────────┐          │
│   │       通  过        │ │     不 通 过        │          │
│   └────────────────────┘ └────────────────────┘          │
└──────────────────────────────────────────────────────────┘
```

点「通过」→ **零输入直接提交**，卡片右滑出、下一条推入。通过的图片可以后补。

### 9.3 不通过态（原地展开，不跳页）

```
┌──────────────────────────────────────────────────────────┐
│ B-003 · 节点参数                        [ 标记为不通过 ]   │
│──────────────────────────────────────────────────────────│
│ 说明 必填                                                 │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ 点「导出」没反应，列表还是原来的数据                    │ │
│ └──────────────────────────────────────────────────────┘ │
│ 控制台报错  选填                                          │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ Uncaught TypeError: Cannot read properties of undefined│ │
│ └──────────────────────────────────────────────────────┘ │
│ 截图  选填 · 在卡片上直接 Ctrl+V 即可                      │
│ ┌────┐ ┌────┐ ┌───────┐                                  │
│ │IMG │ │IMG │ │ 粘贴  │                                  │
│ └────┘ └────┘ └───────┘                                  │
│                                                          │
│ 提交后同时写入：测试流程记录 1 条 + bug 报告 1 条           │
│──────────────────────────────────────────────────────────│
│ [ 确认提交并写下一条 ]                    [   取消   ]     │
└──────────────────────────────────────────────────────────┘
```

### 9.4 键盘映射（全功能可键盘完成）

| 键 | 动作 |
|---|---|
| `Enter` | 通过（主态）／提交（表单态，需说明非空） |
| `Backspace` | 不通过 → 展开表单 |
| `Ctrl+B` | **跳过** —— 这条本轮不测，直接下一条（见 9.7） |
| `←` `→` | 上一条 / 下一条 |
| `Ctrl+Z` | 回退上一条并重新打开（本地回退，见第六节） |
| `Esc` | 收起表单，不提交 |
| `Ctrl+V` | 任意位置粘贴截图（主态自动展开表单） |
| `Ctrl+P` | 切置顶（PiP 开/关） |
| `?` | 快捷键帮助浮层 |

### 9.5 视觉规范（实现时照抄）

| 项 | 值 |
|---|---|
| 卡片圆角 / 内部元素圆角 | 12px / 8px |
| 边框 | 0.5px，`rgba(0,0,0,.15)` |
| 字号 | 标题 15 / 正文 13 / 次要 12 / 标签 11 |
| 字重 | 只有 400 与 500，不用 600/700 |
| 通过 | 底 `#E1F5EE`，字 `#0F6E56` |
| 不通过 | 底 `#FCEBEB`，字 `#A32D2D` |
| 负向断言 / 待确认 | `#BA7517` |
| 进度：已过 / 当前 / 未到 | `#1D9E75` / `#EF9F27` / `#D3D1C7` |
| 行高 | 正文 1.6 |
| 动画 | 只做 `transform` 与 `opacity`，≤180ms；跟随 `prefers-reduced-motion` |

### 9.6 其它必须有的小东西

- **提交中禁用按钮 + 转圈**，防止连点写出两条。
- **已提交列表**（顶栏点开）：从 `progress.json` 渲染，点任意一条可跳回去改；改完重提交走覆盖。
- **失败不吞**：任何一步失败，卡片底部出现红条 + 完整错误 + `hint`，并提供「重试」按钮。
- **图片缩略图带 ×**，可单张删。

### 9.7 「跳过」（= 写入「未执行」）

**定位**：这条用例**本轮不测**。不是发现了缺陷，是「这次不测它」。

| | 不通过 | 跳过 |
|---|---|---|
| 写测试流程记录 | ✓ 结果 = `不通过` | ✓ 结果 = **`未执行`** |
| 写 bug 报告 | ✓ | **✗** |
| 要不要填说明 | 必填 | 不用填 |
| 界面位置 | 主按钮（右边，红色） | `Ctrl+B` + 一个**次要小按钮**，别放主按钮区 |

**为什么写「未执行」而不是「阻塞」**：枚举里两个值语义不同 ——
「未执行」是「这轮没跑」，正是跳过要表达的意思；
「阻塞」听起来像「被环境卡住，想跑跑不了」，会让人误以为存在待解决的障碍。
用前者，看表的人不会多问一句。

**不写 bug 报告的理由**：没测就没有缺陷。写进 bug 表只会让那张表变脏，
以后按「进展状态」看板派活时会多出一堆无意义的条目。

**为什么不做成第三个主按钮**：三个主按钮会让人在赶进度时误点。
跳过是个低频动作用快捷键 + 次要位置更合适。

#### 跳过不是终态 —— 之后随时能改

跳过的记录**照样可以重新打开改回来**，走的是和别的结果完全一样的覆盖路径：

- `未执行` → `通过` / `不通过`：属于「有值 → 有值」，直接提交新值即可，第八节的三道护栏自动适用
- 于是「已提交列表」里跳过的条目**不能置灰**，必须和别的条目一样可点、可改、可重提交
- 唯一例外：由 `未执行` 改成 `不通过` 时，会**新写一条** bug 报告（之前没写过，`bug_record_id` 为空 → 走 create）
- 反向由 `不通过` 改回 `未执行` —— **bug 报告不动**，不自动删（8.6 的规矩）

---

## 十、置顶方案（本设计的重点）

### 10.1 方案 A：Document PiP（零安装，首选）

浏览器原生能力，Chrome/Edge 116+ 桌面版即可，**不需要装任何东西**。

**做什么**：页面顶栏放一个「置顶」按钮。点一下，整张卡片连同一份 CSS 搬进系统级浮窗，那个浮窗**浮在所有窗口之上**、无边框、可拖拽可缩放。

**实现要点**（这几条是实现成败的关键）：

1. **必须在点击的同步调用栈里发起**，中间不能有 `await`：
   ```js
   btn.addEventListener('click', () => {
     documentPictureInPicture.requestWindow({ width: 420, height: 760 })
       .then(pip => { /* 到这一步才可以 await */ });
   });
   ```
   中间插了异步，Chrome 会判为「非用户手势」，直接拒绝。

2. **能力探测**：`if (!('documentPictureInPicture' in window))` 就隐藏按钮，降级成普通窗口。

3. **CSS 复制**：遍历 `document.styleSheets` 把规则注入浮窗的 `<head>`；`<link>` 形式的样式表要等 `load` 再注入，否则首帧是裸的。

4. **DOM 搬运**：把卡片容器 `append` 进 `pipWindow.document.body`；监听 `pipWindow.addEventListener('pagehide')` 搬回主标签页。这样关闭浮窗后界面自动复原。

5. **事件监听要装到两个 document 上**。`paste` 和 `keydown` 在哪个 document 触发就在哪个 document 冒泡 —— 搬过去之后主标签页的监听收不到，必须补装。

6. `disallowReturnToOpener: true` 可以隐藏浮窗里的「返回标签页」小按钮，界面更干净。

**为什么它是首选**：零安装、真原生置顶、正好匹配「卡片 + 两个按钮」这种小工具形态。而且主标签页和浮窗是**同一份 JS 状态**，主标签页可以顺理成章地当控制台（已提交列表、改错、设置）。

**必须知道的四个限制**（都是可接受的，但要提前知道）：

| 限制 | 实际影响 |
|---|---|
| 窗口位置网页设不了 | 手动拖一次到顺手的位置，Chrome 会记住 |
| 尺寸有上下限 | Chrome 会 clamp 过小/过大的请求，不能精确到像素 |
| 生命周期不超出主标签页 | 主标签页一关，浮窗跟着消失 |
| 浮窗内不可导航 | 单页应用无感；别在浮窗里放会跳转的链接 |

### 10.2 方案 B：pywebview 原生窗（长期主力）

```python
# app.py 的全部核心
import threading, webview
from serve import serve_in_thread          # 复用同一个后端

threading.Thread(target=serve_in_thread, daemon=True).start()
webview.create_window(
    "执行器", "http://127.0.0.1:8765",
    width=420, height=900, x=screen_w - 444, y=24,
    on_top=True,           # ← 常驻置顶
    frameless=True,        # ← 无边框，像个浮层
    easy_drag=True,        # ← 顶部留一条可拖区
    resizable=True, min_size=(360, 600),
)
webview.start()
```

要点：

- **无边框后要自己画标题栏**，否则窗口没法关。放三个小按钮（置顶 / 最小化 / 关闭），走 `js_api` 回 Python。
- **拖拽区**：pywebview 4.x 的约定是给元素加 `pywebview-drag-region` class，配合 `easy_drag=True`。（动手时按装的版本确认一次 class 名。）
- **位置**：用 `webview.screens[0]` 拿屏幕尺寸，自己算「贴右侧」的坐标 —— 这是 PiP 给不了、而原生窗能给的能力。
- **`on_top` 运行时切换**：pywebview 4.x 的 `Window.on_top` 是 get/set 属性，可以直接切。（动手时先验证一次。）
- **后端复用**：`serve.py` 抽一个 `serve_in_thread()`，浏览器模式和原生窗模式共用同一份，不写两遍。

### 10.3 方案 C：PowerToys「始终置顶」（应急回退）

`Win+Ctrl+T` 把任意窗口钉住，微软官方、免费。**零代码**。
适合：PiP 和 pywebview 都还没弄好、但今天就要测。放在文档里当保险，不作为主路径。

### 10.4 明确不选的

- **Electron** —— 见 D1。
- **自写 Chrome 扩展调 `chrome.windows`** —— `alwaysOnTop` 在现代 Chrome 的扩展 API 里已不可靠（历史遗留，主要面向已废弃的 Chrome Apps）。为置顶去写扩展是杀鸡用牛刀，且随时可能被上游砍掉。

### 10.5 三条路的关系

不互斥。前端只有一份，所以：

```
今天        方案 A（PiP）         零安装，先跑通
日常        方案 B（pywebview）   装一个包，常驻置顶 + 固定位置
应急        方案 C（PowerToys）   什么都能钉
将来要分发  方案 D（Tauri）        打包 exe，前端原样复用
```

---

## 十一、状态机与错误处理

### 11.1 前端状态

```
idle ──点通过/提交──▶ submitting ──成功──▶ idle（cursor 前进）
                          │
                          └──失败──▶ error（红条 + hint + 重试）
                                        │
                                        └──重试──▶ submitting
```

`submitting` 期间：两个按钮 `disabled`，全局屏蔽 `Enter` / `Backspace`，防止连点写两条。

### 11.2 错误分级

| 类型 | 表现 | 动作 |
|---|---|---|
| 网络断（fetch 抛错） | 「连不上本地服务，serve.py 还在跑吗」 | 重试 |
| Lark token 失败 | 后端返回 `hint`（`explain()` 已经翻译好了） | 按 hint 去修，重试 |
| 权限未生效 | `99991672` → 「去发布一个新版本」 | 按 hint 去修 |
| 写入部分成功 | 返回 `partial`，前端存下 `record_id` | 重试走覆盖，不产生重复 |
| 图片超 20MB | `1061043` | 前端先拦一道，提示压缩 |

**原则：错误信息永远是人话 + 下一步动作，不抛栈。** `lark_client.py` 的 `explain()` 已经把常见错误码翻译好了，直接透传。

---

## 十二、工程约定与已知坑

### 环境

- **沙箱会拦 WSL**（`PROGRAM BLOCKED`）→ 一律用 Windows 那个装了 `requests` 的 Python。
- **Bash 工具 PATH 是坏的**：`dirname` / `cat` / `tail` 都不存在。命令前先
  `export PATH="/usr/bin:/bin:/c/Program Files/Git/usr/bin:$PATH"`，并且**别用管道收尾**。
- **写文件用写文件工具**，别用 `cat > f <<EOF`（静默失败，建出来是空的）。
- 端口写死 **8765**；被占用时自动顺延到 8766/8767，并把实际端口打到控制台和 `start.bat` 的窗口标题上。

### Lark（都是踩过的）

- 权限「已开通」≠「已生效」，生效开关是**发布新版本**。
- 日期传**毫秒**；同一张表**不支持并发写** → 后端串行，两步之间留 0.3~0.6s。
- 附件**必须两步**：先 `upload_media` 换 `file_token`，再写记录；`file_token` 只在当前 base 有效。
- 人员字段只认 `open_id`，且必须是同一个应用 token 读出来的。

### 代码

- `Edit` 工具对同一文件并行发多个编辑会丢改动 → **串行改**。
- **别写「自动删除不在预期内的记录/字段」这类逻辑。** 2026-09-15 就是它删掉了用户自加的字段。发现重复只提示，不自动删。
- 前端不引任何框架、不引 CDN：断网也要能用。

---

## 十三、里程碑

| # | 交付 | 完成标志 |
|---|---|---|
| 1 | `serve.py` + `/api/health` | 控制台打印「两张表都读得到，字段名全对」 |
| 2 | `/api/session` + 只读卡片 | 浏览器里能翻完 14 条，长断言滚得动 |
| 3 | **接「通过」** | **点一下 → 3 秒内 Lark 出现一行，八个字段全对** ← 分水岭 |
| 4 | 接「不通过」 | 展开表单 → 同时写记录 + bug；人员字段解析成「Max」 |
| 5 | 贴图 | Ctrl+V → 缩略图 → 提交 → Lark 里能看到、两张表都有 |
| 6 | `progress.json` + 覆盖护栏 | 关掉重开进度还在；重提交是覆盖不是追加；**空覆盖三连测全过**（见第十四节） |
| 7 | 两个外壳 | `start.bat` 开浏览器；`start-desktop.bat` 开原生置顶窗 |

第 3 步之前的风险都在「本地」，第 3 步验证的是「整条路通不通」。过了第 3 步，后面全是增量。

---

## 十四、验收标准

- [ ] 双击 `start.bat`，浏览器自动打开，14 条用例能翻
- [ ] 点「通过」→ 3 秒内 Lark 出现对应一行，字段全对
- [ ] **「日期」列显示 `2026/09/15`，不带时分秒**
- [ ] **「报告人」列显示 `Max`**
- [ ] 点「不通过」填说明 + Ctrl+V 贴图 → Lark 里截图能看到、控制台在「控制台」列、bug 报告多一条挂在 Max 名下
- [ ] **`Ctrl+B` 跳过一条 → 测试流程记录「结果」= `未执行`，bug 报告里 `不新增` 任何行**
- [ ] **跳过的那条在「已提交列表」里仍可点开，改成「通过」→ 表里那条原地更新，不新增行**
- [ ] **跳过改成「不通过」→ 正常写一条 bug 报告（之前没写过，走新建）**
- [ ] 中途关掉再打开，进度还在，测过的不丢

**覆盖（重点验，这是第 6 条要求的核心）：**

- [ ] 同一条重复提交 → 覆盖，不是追加（表里始终只有一行）
- [ ] 提交一条「不通过」（带控制台 + 图）→ 再提交同一条、只改结果
      → **「控制台」列和图原样保留**（没填的字段没被清）
- [ ] 提交一条带图的 → 重提交时**不传图**
      → **Lark 里的图还在**
- [ ] 在表里手动把某条记录的「控制台」改成一句话 → 本地重提交
      → **提示「云端与本地不一致」，且以云端为准**，不盲目覆盖
- [ ] 什么都没改直接重复提交 → **零网络请求**（看后端日志，不打 Lark）

**外壳与键盘：**

- [ ] **点「置顶」→ 卡片浮在被测系统窗口之上，可拖可缩放**
- [ ] **双击 `start-desktop.bat` → 无边框原生窗，常驻置顶，贴在屏幕右侧**
- [ ] **纯键盘走完 5 条用例：Enter / Backspace / Ctrl+B / ← → / Ctrl+Z**
- [ ] 拔网线提交 → 出现人话错误 + 重试按钮，不白屏

---

## 十五、决策台账（全部已定，可开工）

### 已确认

| # | 事项 | 定论 |
|---|---|---|
| 1 | 日期精度 | **只到日**（`2026/09/15`）。已只读核实现有两张表的日期字段格式就是 `yyyy/MM/dd`，**不用改表**；只需代码端改传「当天正午的毫秒」。见 7.3 |
| 2 | 「报告人」填谁 | **Max**。上游 `HANDOFF-RUNNER.md` §6 是对的；`.env` 里的 `DEFAULT_REPORTER=Lucas` 是错的（那是先来的同事），**已改** |
| 3 | 跳过 | 按钮文案「**跳过**」，写入「结果」= **`未执行`**，**不写 bug 报告**，不填说明。**跳过不是终态，之后可改回**。见 9.7 |
| 4 | 测试流程记录要不要加说明列 | **不加列**。该表只用「结果」列的单选标签回答过没过，够用；细节去 bug 报告查 |
| 5 | 重提交覆盖 | **update + 只传变化字段 + 三道护栏**。空覆盖从机制上不可能发生。见第八节 |
| 6 | 结果枚举 | 沿用现表，不动。已核对：「结果」有 `通过/不通过/阻塞/未执行`，bug「优先级」只有 `P0/P1/P2`（所以 P3 要归到 P2） |

### 视觉参数 —— 先按默认做，跑起来再调

这三项**不阻塞开工**，先照下表实现；看着不对再改，都是一处常量的事。

| # | 事项 | 默认值 | 在哪改 |
|---|---|---|---|
| V1 | 原生窗常驻位置 | 贴屏幕右侧、宽 420、上边距 24px | `app.py` 里 `create_window()` 的参数 |
| V2 | 卡片宽度 | 浏览器浮窗 420px；主标签页自适应 | `static/style.css` 的 `--card-width` |
| V3 | PiP 浮窗初始尺寸 | 420 × 760 | `app.js` 里 `requestWindow()` 的参数 |

> 定完这三项再回头补文档，不着急。

---

## 附：与上游文档的差异

| 处 | `HANDOFF-RUNNER.md` | 本文 | 原因 |
|---|---|---|---|
| 重复提交 | 先删旧记录再写新的 | update + 只传变化字段 + 三道护栏 | 少一次调用、无数据空洞、`record_id` 稳定；且**空覆盖从机制上不可能** |
| 日期取值 | 传毫秒（`ts_ms()`，当前时刻） | 传**当天正午**的毫秒 | 字段是「只到日」，正午避开日界，防止深夜/凌晨提交显示成前后一天 |
| 阻塞 | 不通过 / 阻塞 **都**提 bug | **只有不通过提** bug；「跳过」写入 `未执行` | 跳过 = 这轮不测，没测就没有缺陷；且「未执行」比「阻塞」语义准 |
| 测试流程记录 | 「用例」列不通过时换行追加说明 | 只写 `编号 + 标题`，不加列 | 该表只用「结果」标签回答过没过，细节归 bug 报告 |
| 报告人 | 文本「Max」 | 「Max」**（沿用上游）**，同时**修掉了 `.env` 里的 `Lucas`** | 上游是对的；`.env` 那处是错的 |
| 置顶 | 未解决（当时认为浏览器做不到） | Document PiP + pywebview 双壳 | 浏览器 116+ 已原生支持 always-on-top 浮窗 |
| 快捷键 | 未提 | 全功能键盘映射（含 `Ctrl+B` 跳过） | 追求「读完就敲键」，手不离键盘 |

除以上七处，上游的结论全部沿用。

---

## 附：本次核对留下的实证

| 结论 | 怎么得到的 |
|---|---|
| 两张表的日期字段已是 `yyyy/MM/dd`（只到日） | 只读跑 `build/inspect_date_fields.py`，读 `list_fields` 的 `property.date_formatter` |
| 「结果」枚举含 `通过/不通过/阻塞/未执行`；bug「优先级」只有 `P0/P1/P2` | 同上，读 `property.options` |
| 更新记录是**部分更新**（未传字段保持不变） | 飞书 Bitable 官方 SDK 文档原文 + 多篇实践说明一致 |
| 「截图」是附件字段（type 17）、「反馈人 / 跟进人」是人员字段（type 11, multiple） | 同 `list_fields` |
